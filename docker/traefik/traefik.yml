api:
  dashboard: true
  debug: true

log:
  level: DEBUG



entryPoints:
  http:
    address: ":80"
  https:
    address: ":443"


providers:
  file:
    directory: /config/
    watch: true
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
